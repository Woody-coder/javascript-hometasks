let a = 1, b = 1;

let c = ++a; // будет 2 (если задать алерт - выдаст так же 2, потому что префикс)
let d = b++; // будет так же 2 (если задать алерт - выдаст 1 (старое значение), потому что постфикс)
